# timerDemoUsingInterrupts
This demo program shows how to set up an Interrupt Service Routine (using HAL) that will get called periodically every n seconds (for a specified value of n) on an Arduino Giga R1 board.
